# 실제 값은 quiz-app/.env 에 두세요(.gitignore 처리됨, 레포에 커밋되지 않음).
# docker compose가 같은 디렉터리의 .env를 읽어 docker-compose.yml의 ${...}를 채웁니다.
DB_HOST=
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=quiz-app
# 관리자(/admin) 최초 시드 비밀번호. 이후 /admin에서 변경하면 DB에 저장됨.
ADMIN_PASSWORD=
